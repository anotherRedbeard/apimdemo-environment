<!--
    - Policies are applied in the order they appear.
    - Position <base/> inside a section to inherit policies from the outer scope.
    - Comments within policies are not preserved.
-->
<!-- Add policies as children to the <inbound>, <outbound>, <backend>, and <on-error> elements -->
<policies>
	<!-- Throttle, authorize, validate, cache, or transform the requests -->
	<inbound>
		<base />
		<set-variable name="originalUrlPath" value="@(context.Request.OriginalUrl.Path)" />
		<set-variable name="UrlPath" value="@(context.Request.Url.Path)" />
		<set-variable name="apiPath" value="@(context.Api.Path)" />
		<set-variable name="urlPathLength" value="@(context.Request.OriginalUrl.Path.Length)" />
		<set-variable name="apiPathLength" value="@(context.Api.Path.Length)" />
		<rewrite-uri template="@("/" + context.Api.Version + "/" + context.Request.Url.Path)" copy-unmatched-params="true" />
	</inbound>
	<!-- Control if and how the requests are forwarded to services  -->
	<backend>
		<base />
	</backend>
	<!-- Customize the responses -->
	<outbound>
		<base />
	</outbound>
	<!-- Handle exceptions and customize error responses  -->
	<on-error>
		<base />
	</on-error>
</policies>